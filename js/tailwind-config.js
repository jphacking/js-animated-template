// Tailwind configuration file
// This script must be loaded before the Tailwind CDN script
tailwind.config = {
  theme: {
    extend: {
      // Add custom theme values here if needed
    },
  },
};
